# M2 玩法冒烟验收

## 状态与范围

本文保留完整的 14 项 M2 验收用例及 15 分钟连续玩法要求，不是整体通过报告。**M2-A 已取得桌面端自动测试和部分交互证据，但 14 项综合用例尚未完整验收，15 分钟连续玩法、性能基线和指定 Y9000P 笔记本验收未执行。** 构建成功或局部动作有响应不能替代整项验收。

M0 仅做过局部启动观测和一次 Release Esc 退出，见 [M0 报告](../milestones/m0/README.md)。这些历史记录保留；M2-A 当前结果与证据见 [M2-A 报告](../milestones/m2-a/README.md) 和本文的局部证据表，不用旧版本结果替代当前复测。

M2 目标是保住现有单机体素玩法，并得到能够正常启动、交互、退出的游戏版本。当前世界为固定的 441 个已创建区块，外围一圈不绘制，内圈最多 361 个区块参与绘制，不是无限地图。存档、联网、动态区块流式加载和完整生存系统不因本表而自动进入 M2 范围。

M0 语义基线见 [架构与数据流](../architecture/current-data-flow.md)；M2-A 当前变化见 [玩家循环](../architecture/player-loop.md) 与 [应用生命周期](../architecture/application-lifecycle.md)。重点代码为 [主循环与按键](../../src/core/application.cpp)、[射线编辑](../../src/playercontroller/playercontroller.cpp)、[角色](../../src/core/ECS/Systems/character_system.cpp)、[碰撞](../../src/core/ECS/Systems/physics_system.cpp)。

## M2-A 局部证据

以下只记录已经取得的桌面端证据，不将局部动作提升为完整用例通过。用户已要求停止操作游戏界面，后续本轮仅整理验收报告与交付材料。

| 范围 | 已观察或实测结果 | 证据与仍缺少的部分 |
| --- | --- | --- |
| 自动测试 | Debug、Release 各 9 项测试通过 | 见 [M2-A 报告](../milestones/m2-a/README.md) 的构建测试证据；不是 14 项人工玩法通过 |
| 有限帧桌面启动 | 最终 Debug 探针完成 120 帧，退出码 `0`，GL 诊断计数 `0` | [探针结果](../milestones/m2-a/evidence/debug-final-120/result.json)；不证明完整视角、窗口与长时玩法 |
| 基础交互 | 安装目录 Release 已观察少量移动、跳跃落地和 E 键切换材料 | [跳跃画面](../milestones/m2-a/evidence/release-input-retest/02-jump.png)、[落地移动画面](../milestones/m2-a/evidence/release-input-retest/03-landed-moved.png)、[运行日志](../milestones/m2-a/evidence/release-input-retest/stdout.log)；未完整覆盖各方向、斜向速度、奔跑、重复跳跃和边界接触 |
| 鼠标编辑 | 首次交互记录中有拆除和放置后的截图 | [拆除画面](../milestones/m2-a/evidence/release-interactive/02-removed.png)、[放置画面](../milestones/m2-a/evidence/release-interactive/03-placed.png)；未覆盖完整材料循环、持续按键、双键和区块交界 |
| 进程正常结束 | `release-final-exit` 记录约 `106.164` 秒、`7500` 帧、退出码 `0`，未超时 | [退出记录](../milestones/m2-a/evidence/release-final-exit/result.json)；电脑操作被用户 Esc 停止后仅等待进程结束，不能归因为自动 Esc 用例完成，也未完成窗口关闭与重启的整组用例 |
| 真实资源损坏 | 非法 shader、损坏 PNG、空 YAML 三个隔离副本均按预期退出码 `3` 结束 | [故障汇总](../milestones/m2-a/evidence/debug-faults/summary.json)；不支持图形能力等其他失败场景仍待验收 |

初次存在键盘未响应的观察，后续切换英文输入法后按键有响应。期间输入实现与测试条件也发生变化，证据不足以把初次现象单独归因于粘滞按键或输入法；不得据此宣称所有焦点与输入问题已解决。

## 执行前记录

每次验收使用同一份记录；不知道的数据填“未记录”，不得用推测替代。

| 字段 | 本次值 |
| --- | --- |
| 日期、执行者、提交或源码快照 | 未记录 |
| 操作系统、CPU、内存 | 未记录 |
| GPU、驱动、实际 OpenGL 版本 | 未记录 |
| 构建工具、编译器、Debug / Release | 未记录 |
| 可执行文件和启动工作目录 | 未记录 |
| 分辨率、窗口模式、VSync | 未记录 |
| 世界规模和是否有测试场景输入 | 未记录 |
| 资源包位置与启动日志位置 | 未记录 |
| 截图/录像、帧时间和内存记录位置 | 未记录 |

当前代码请求 OpenGL 4.6 Core 并默认打开 VSync，实际支持情况需要启动后记录。当前日志 seed 不能完整复现世界，不得仅凭相同 seed 宣称场景一致；确定性输入尚未实现时记录截图、路线和测试位置，并说明限制。

## 功能用例

“预期”是验收目标，不是当前游戏已实现或已验证的描述。失败时记录复现步骤、位置、持续时间、日志及画面，保留原结果再进行修复复测。

| ID | 操作 | 验收预期 | 最低证据 | 当前结果 |
| --- | --- | --- | --- | --- |
| M2-01 启动与资源 | 从交付目录启动，不依赖 IDE；再从约定的另一工作目录启动 | 窗口出现，地形和纹理可见，无空白画面；资源定位符合交付约定 | 完整启动日志、世界画面、启动位置 | 未完整验收，见 M2-A 局部证据 |
| M2-02 出生与落地 | 进入世界后不输入，观察玩家直到落地，再静止至少 20 秒 | 出生不嵌入实心方块；能落地且不穿地、漂移或无限下落 | 从进入世界到稳定落地的录像 | 未完整验收，见 M2-A 局部证据 |
| M2-03 基本移动 | 依次按 W/S/A/D，松开；直走、斜走；按住左 Shift 跑 | 方向与视角一致；松键停止水平移动；斜向不额外加速；奔跑快于步行 | 同一路段录像；可记录位移和时间 | 未完整验收，仅观察少量移动 |
| M2-04 跳跃 | 在平地按 Space；跳起后再次按；落地后再跳；走下台阶后尝试跳 | 平地起跳和落地稳定；不会获得意外空中跳跃；落地后可再次起跳 | 完整动作录像及异常发生位置 | 未完整验收，仅观察跳跃落地 |
| M2-05 碰撞 | 正对墙、斜对墙、墙角移动；跳起撞低顶；落到单方块顶面 | 不穿墙、不穿顶、不被持续弹开；站立与墙角接触稳定 | 包含边界接触的录像 | 未执行 |
| M2-06 视角与窗口 | 鼠标转向、上下看、滚轮缩放；调整窗口尺寸 | 视角方向正确，俯仰有限制；FOV 不异常；比例和视口正确，无不可恢复黑屏 | 调整前后截图及短录像 | 未执行 |
| M2-07 射线与选择框 | 从不同面观察方块；靠近和远离；快速转向后点击 | 选择框对应当前可交互方块；大于约 3 单位不编辑；不选中背后的遮挡方块 | 目标、距离条件和点击结果录像 | 未完整验收，已有局部编辑截图 |
| M2-08 方块选择与编辑 | Q/E 完整循环；左键拆除、右键放置；按住鼠标；同时按左右键 | 8 种类型循环无越界；放置在命中面的相邻格；移除正确目标；持续操作受约 0.2 秒防抖控制；结果不损坏世界 | 每类至少一次放置、拆除录像及日志 | 未完整验收，仅观察 E 切换及首次拆放 |
| M2-09 区块交界 | 在 x 或 z 为 16 的整数倍附近拆放；同时覆盖正坐标、负坐标及四块交点 | 不崩溃、不改错块；两侧暴露面和遮挡及时更新，无持久裂缝 | 修改前后截图、区块交界位置、操作顺序 | 未执行 |
| M2-10 高度与地图外围 | 在可达边界检查最底部、最高层附近和固定地图外围；必要时使用专门测试入口 | 越界查询/编辑不崩溃、不破坏内存；地图不被误认为无限；离开可玩区的限制或回退策略明确 | 边界位置、日志、画面及采用的边界策略 | 未执行 |
| M2-11 调试无碰撞模式 | 按住 Caps Lock 移动，配合左 Ctrl 下降，然后松开 Caps Lock | 按住期间按现有逻辑关闭重力/碰撞；松开后恢复；恢复时不留永久失效状态。不得把 Caps Lock 键盘灯或开关状态当作模式切换 | 按下、保持、释放的连续录像 | 未执行 |
| M2-12 长帧与焦点 | 拖动窗口、切出再切回、最小化约 10 秒后恢复；分别在静止和移动时检查 | 无崩溃、无限追赶或持续卡死；输入不保持失控状态，画面可恢复 | 恢复前后录像、长帧/日志记录 | 未执行 |
| M2-13 正常退出 | 分别以 Esc 和窗口关闭按钮退出，再启动 | 能及时正常退出并再次进入；无崩溃；可用的内存/GL 调试信息无未解释错误 | 每次退出日志、退出状态、重启画面 | 未完整验收；已记录正常退出，但自动 Esc 因果未确认 |
| M2-14 启动失败处理 | 在隔离的测试发布目录缺失必要资源；在可行环境模拟不支持的图形能力 | 明确报告失败原因并安全退出，不继续进入无效的主循环；不修改原始资产验证故障 | 故障输入、错误日志及退出状态 | 未完整验收；资源预检与三类损坏故障有独立证据 |

M2-10 不要求新增无限世界。若没有安全到达极端高度的入口，应使用局部测试补足底层边界验证，人工玩法项标注未执行及原因，不能冒充通过。M2-11 也不要求添加完整的上升飞行控制。

## M2：15 分钟连续玩法

开始计时前已经成功进入可操作世界；加载耗时单独记录。以下步骤连续运行至少 15 分钟，不用重启绕过异常。可先完成上表其余用例，再执行本段。

| 时间段 | 场景 | 观察项 | 当前结果 |
| --- | --- | --- | --- |
| 0-3 分钟 | 步行、奔跑、转向、跳跃、落地 | 输入、碰撞、帧时间与内存起始值 | 未执行 |
| 3-7 分钟 | 重复拆放、切换材料、连续操作 | 编辑反馈、网格重建卡顿、错误日志 | 未执行 |
| 7-10 分钟 | 正负坐标区块交界与墙角交互 | 裂缝、旧面残留、错误坐标、碰撞一致性 | 未执行 |
| 10-12 分钟 | 改变窗口尺寸、切出和恢复 | 恢复是否及时、输入是否失控 | 未执行 |
| 12-15 分钟 | 回到先前场景继续操作，末尾静止观察 | 画面和玩法仍正常；比较相近场景的内存及帧时间 | 未执行 |

通过条件：无崩溃、卡死、世界损坏或阻止继续游玩的故障；正常退出；日志中的错误均有解释和处置。帧时间、内存峰值与变化需记录，但未约定硬件、场景和预算前，不用任意 FPS/内存数字宣布“工程级性能达标”。

15 分钟通过只证明短程稳定性，不证明不存在泄漏。未执行、受阻或出现异常的项目必须保留在结果摘要中。

## 后续：60 分钟稳定性

这是后续稳定性验收，不与 M2 的 15 分钟合并计时，也不能由 M2 通过推定通过。

| 用例 | 操作与观测 | 通过依据 | 当前结果 |
| --- | --- | --- | --- |
| ST-60 长时间循环 | 连续 60 分钟重复移动、编辑、交界、静止和窗口恢复；每 5 分钟记录相同场景的内存、帧时间与错误数 | 无崩溃、卡死和世界损坏；相同场景资源使用无无法解释的持续增长；性能符合届时约定预算 | 未执行 |
| ST-RESTART 重启循环 | 多次启动、进入、操作、正常退出，记录次数和退出状态 | 无偶发启动/退出故障；资源检查无未解释问题 | 未执行 |

若未来加入动态区块卸载、保存/加载或异步网格，应新增对应生命周期、取消任务和旧结果丢弃用例。当前这些能力未接入主循环，不把它们列为现有实现的已通过功能。

## 结果填写规则

完整执行的项目写“通过”或“失败”；未执行或只做了部分操作的项目写“未执行（原因）”或“未完整验收（已做部分及缺项）”，并附证据路径。修复后的复测应引用原失败记录。建议结果摘要分别列出构建结果、启动结果、功能结果、15 分钟结果和后续 60 分钟结果，避免一个“测试通过”掩盖范围差异。
